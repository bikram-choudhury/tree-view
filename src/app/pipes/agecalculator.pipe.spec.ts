import { AgecalculatorPipe } from './agecalculator.pipe';

describe('AgecalculatorPipe', () => {
  it('create an instance', () => {
    const pipe = new AgecalculatorPipe();
    expect(pipe).toBeTruthy();
  });
});
